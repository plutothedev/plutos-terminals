// (C) MCP client for the native agent. See docs/superpowers/specs/2026-06-14-mcp-native-agent-design.md.
// Uses the official `rmcp` SDK (adopted in the A1 spike) for transports + protocol.
pub mod config;
pub mod client;
pub mod manager;
