# dependencies
node_modules/

# build output
dist/
src-tauri/target/
src-tauri/gen/

# user runtime state — scrollback, transcripts, store.json
# (lives next to package.json in dev mode; %APPDATA% in production)
data/

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / OS
.DS_Store
.vscode/
.idea/
*.swp
Thumbs.db

# env
.env
.env.local
.env.*.local

# tauri build artifacts
src-tauri/Cargo.lock.bak

# 2026-08-21 audit: the security team report and the parsed workflow dump carry a
# byte-level trigger for an unfixed VNC crash (SEC-2) and stay private (the
# vault's security backlog holds them); the eslint baselines are 2.3 MB of
# absolute local paths. The rest of docs/audit-2026-08-21/ is public.
docs/audit-2026-08-21/teams/security.md
docs/audit-2026-08-21/wf-parsed.json
docs/audit-2026-08-21/00-baseline/eslint.json
docs/audit-2026-08-21/00-baseline/eslint-after.json
docs/audit-2026-08-21/00-baseline/knip.err
